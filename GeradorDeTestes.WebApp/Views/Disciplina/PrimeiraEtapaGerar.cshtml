@model PrimeiraEtapaGerarDisciplinasViewModel

@{
    ViewBag.Titulo = "Geração Automática de Disciplinas";
}

<hr class="border border-white opacity-25" />

<div class="row justify-content-center my-5">
    <div class="col-md-8">
        <div class="card rounded-4 border-0">
            <div class="card-body cor-padrao p-md-4">
                <form asp-action="PrimeiraEtapaGerar" method="post">
                    <h3 class="mb-4">Geração Automática de Disciplinas</h3>

                    @if (ViewData.ModelState["ConflitoGeracao"]?.Errors.Count > 0)
                    {
                        <div class="alert alert-danger alert-dismissible fade show my-3" role="alert">
                            @ViewData.ModelState["ConflitoGeracao"]!.Errors.First().ErrorMessage
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <div class="form-group d-grid gap-2 mt-3">
                        <label asp-for="QuantidadeDisciplinas" class="control-label fs-5">Quantidade de Disciplinas:</label>
                        <input asp-for="QuantidadeDisciplinas" class="form-control" type="number" />
                        <span asp-validation-for="QuantidadeDisciplinas" class="text-danger"></span>
                    </div>

                    <div class="form-group d-flex align-items-center justify-content-end gap-3 mt-3">
                        <a href="/disciplinas" class="botao-excluir botao-excluir-outline rounded-3 px-2">
                            <i class="bi bi-x-circle"></i>
                            Cancelar
                        </a>
                        <button type="submit" class="botao rounded-4 px-5">
                            <i class="bi bi-arrow-right-circle"></i>
                            Continuar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
